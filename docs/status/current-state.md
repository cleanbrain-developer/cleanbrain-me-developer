# Current State

Last updated: 2026-09-20

## Current phase

**Live in production, with a fully working CI/CD pipeline.** `https://developer.cleanbrain.me` is up, and a normal push to `main` now deploys automatically end to end (verified with a real triggered run). This phase's original exit criteria are all met; the next phase is ordinary feature/content work, matching `cleanbrain-me-entrance`'s equivalent milestone.

## Completed

- Chose the repository name `cleanbrain-me-developer` and scaffolded a Next.js (App Router, TypeScript, Tailwind, ESLint, `src/` dir, `@/*` import alias) project with `create-next-app`.
- Adopted a repository-first context structure by following `agent-dev-starter`'s adoption guide in "New project" mode, mirroring `cleanbrain-me-entrance`'s and `relayhub-java`'s prior adoptions: `PROJECT.yaml`, `.ai/constitution/{engineering-principles,agent-behavior,documentation-policy}.md`, `docs/product/{overview,goals,scope}.md`, `docs/architecture/{overview,repository-structure,agent-context-model}.md`, `docs/decisions/ADR-0001-repository-first-context.md`, `docs/infra-required-changes.md`, `CLAUDE.md`, and this file.
- `AGENTS.md`: appended the Codex adapter content below Next.js's own auto-managed `<!-- BEGIN/END:nextjs-agent-rules -->` block instead of replacing it, since `next dev` re-writes that block.
- Recorded ADR-0001 (repository-first context) as accepted, with `cleanbrain.developer` as decider, and an explicit note that where implementation diverges from the original init prompt / design spec, the repository wins.
- Content, adapter-pattern, and demo-safety rules from the source design spec were converted into durable constitution/architecture rules rather than left as prose in this file (see `.ai/constitution/engineering-principles.md` and `docs/architecture/overview.md`).
- Ran the bootstrap acceptance test in a fresh, isolated agent session starting only from `CLAUDE.md` (2026-09-12): all five acceptance questions in `docs/product/goals.md` were answered correctly with repository-path citations, no fabricated content, and no contradictions found across `PROJECT.yaml`, the constitution, product/architecture docs, ADR-0001, and this file.
- Committed the foundation and pushed to the new public GitHub repository `cleanbrain-developer/cleanbrain-me-developer` (`main` branch), with the maintainer's explicit confirmation.
- Implemented Phase 1 (Foundation): dark-first theme tokens in `src/app/globals.css` (no light-mode toggle — a deliberate simplification, not an oversight; see `docs/product/scope.md` if this needs revisiting), `SiteHeader`/`SiteFooter`/`Container` in `src/components/layout/`, a shared `PlaceholderSection` in `src/components/common/`, a skip-to-content link and `:focus-visible` styling in `src/app/layout.tsx`, `prefers-reduced-motion` support, and a routed stub page for every top-level nav item (`/`, `/experience`, `/projects`, `/lab` → redirects to `/lab/relayhub`, `/case-studies`, `/architecture`, `/resume`, `/contact`).
- Verified: `npm run lint` and `npm run build` both pass (all 9 routes prerender as static content). Ran the dev server and checked it with a headless-Chromium screenshot at desktop and mobile (390px) viewports — dark theme, nav, and the `/lab` → `/lab/relayhub` redirect all render and behave correctly; no console errors observed.
- Implemented Phase 2 (Portfolio Core) content and pages: `src/content/{types,profile,experience,projects}.ts` and `src/content/case-studies/{distributed-delivery-race-condition,approval-rollback-timeout,legacy-batch-state,index}.ts`; presentation components under `src/components/{portfolio,project,case-study}/`. Replaced the Phase 1 placeholder stubs on `/`, `/experience`, `/projects`, `/case-studies`, `/architecture`, `/resume`, `/contact` with real content, and added `/projects/[slug]` and `/case-studies/[slug]` dynamic detail routes (`generateStaticParams` over the two current projects and three case studies). `/lab` and `/lab/relayhub` are untouched — still Phase 1 placeholders, on purpose (Phase 3).
- Profile/experience/tech-stack content is sourced from the maintainer's own existing public `cleanbrain-developer/cleanbrain-developer` GitHub profile README, not invented; the Experience/Resume pages present it by focus area rather than a company-name/date timeline, per the maintainer's explicit choice (no fabricated employment history).
- The two Project entries (RelayHub, English Core Speaking) describe real, live projects, using the actual live hostnames from `cleanbrain-me-infra`'s manifests (`relayhub-java.developer.cleanbrain.me`, `english-core-speaking.cleanbrain.me`) and real technical facts already recorded elsewhere (Kafka/Prometheus/admin console for RelayHub; NestJS/Vue3/Prisma/OAuth for English Core Speaking) — no invented metrics or results.
- The three Case Studies are, per the maintainer's explicit choice, composite/abstracted scenarios built from the three topics the original design spec suggested (race condition, external-approval timeout, legacy-batch state) — qualitative only, no fabricated numbers, and each says so explicitly in its own summary.
- `/contact` and `/resume` display the maintainer's real GitHub (`github.com/cleanbrain-developer`) and email (`cleanbrain.developer@gmail.com`), per the maintainer's explicit confirmation to publish both.
- Verified: `npm run lint` and `npm run build` pass (17 routes, including 2 SSG project detail pages and 3 SSG case-study detail pages). Checked `/`, `/projects`, `/projects/relayhub`, `/case-studies`, `/case-studies/distributed-delivery-race-condition`, `/architecture`, `/resume`, `/contact`, and `/experience` in a real browser via headless-Chromium screenshots — all render correctly, no console errors observed.
- Implemented Phase 3 (RelayHub Lab Mock): domain types and the `RelayHubAdapter` interface in `src/lib/relayhub/{types,adapter,scenarios,pipeline}.ts`; `MockRelayHubAdapter` (`src/lib/relayhub/mock-adapter.ts`) as an in-memory, scenario-driven state machine covering all four predefined scenarios (normal; target-500 and timeout, both retrying 3 times before moving to DLQ; validation-error, which fails immediately with no retry); `relayHubLabService` (`src/lib/relayhub/service.ts`) as the single adapter-selection point components depend on, never a concrete adapter or `fetch()` directly.
- Built the Lab UI under `src/components/relayhub/{event-generator,pipeline,metrics,recent-events,event-detail,dlq}/`, composed by the client-side orchestrator `src/components/relayhub/relayhub-lab.tsx`, which now renders at `/lab/relayhub` in place of the Phase 1 `PlaceholderSection`. The Pipeline view collapses multi-attempt delivery retries into one slot (with an "N attempts" badge) for the top-level view, while the full per-attempt timeline and retry history remain visible in Event Detail. A newly generated event's pipeline is revealed stage-by-stage over ~1.8s using the adapter's own real per-event stage results (not a canned animation) — a previously-generated event selected from Recent Events is shown fully resolved, not re-animated.
- `replayDlq` models both outcomes named in the design spec (roughly 70% success / 30% still-DLQ on replay, not hardcoded to always succeed) by appending new delivery/target stage entries to the event's existing timeline rather than discarding retry history.
- Demo-safety constraints from `.ai/constitution/engineering-principles.md` hold structurally, not just by convention: `GenerateEventInput` only accepts the closed `EventType`/`Scenario` unions: there is no code path through which the UI could send an arbitrary URL, header, script, or credential to the adapter.
- Verified: `npm run lint` and `npm run build` pass. Drove the Lab in a real browser (headless Chromium via Playwright, since `chromium-cli` was unavailable in this environment) through all four scenarios plus a DLQ replay — Normal (all stages succeed), Target 500 (3 retries → DLQ, confirmed via screenshot), Replay from DLQ (observed the ~30% failure branch, handled correctly — event stayed `dlq` with a clear replay-failed message and undiminished retry history), and Validation Error (fails immediately, transformation/delivery/target correctly shown `skipped`, no retries). Also checked at a 390px mobile viewport (single-column stacking, no horizontal page scroll; only the Recent Events table scrolls internally, by design). No console errors in any run.
- Implemented Phase 4 (`HttpRelayHubAdapter` interface readiness): `src/lib/relayhub/http-adapter.ts` implements `RelayHubAdapter` against a REST contract documented in `docs/decisions/ADR-0002-relayhub-http-adapter-contract.md` (explicitly provisional — no real backend implements it). `src/lib/relayhub/service.ts` now selects between `MockRelayHubAdapter` (default) and `HttpRelayHubAdapter` via `NEXT_PUBLIC_RELAYHUB_ADAPTER`/`NEXT_PUBLIC_RELAYHUB_API_URL`; neither env var is set in production, so behavior is unchanged. Updated `docs/architecture/overview.md`'s "Adapter pattern" section and `docs/product/scope.md`'s open decisions to match.
- Implemented Phase 5 (Quality) SEO work: `metadataBase`, default OpenGraph/Twitter metadata, and `alternates.canonical` on the root layout (`src/app/layout.tsx`); a `pageMetadata()` helper (`src/lib/seo.ts`) applied to every route's `metadata`/`generateMetadata` export so each page gets its own canonical URL and OpenGraph tags instead of only inheriting the root defaults; a `Person` JSON-LD block on the homepage built from `src/content/profile.ts` (no separately-maintained SEO content). Verified all of this against the actual `next build && next start` production output (not just `next dev`), via `curl` against the rendered HTML — title, meta description, canonical link, OpenGraph tags, and the JSON-LD block are all present with the expected values.
- Added a first automated test suite (previously zero automated tests): `vitest` with 10 unit tests across `src/lib/relayhub/pipeline.test.ts` (the `toPipelineSlots` status-mapper/formatter) and `src/lib/relayhub/mock-adapter.test.ts` (all four scenarios' state transitions, DLQ-replay eligibility and both its outcomes, and metrics aggregation) — the exact test categories design spec §27 "Unit" calls for. `npm run test` runs them; `vitest.config.ts` maps the `@/*` path alias to match `tsconfig.json`.
- **Toolchain note**: `vitest@5` and its pulled-in `vite`/`rolldown` require Node's `node:util` `styleText` export (Node ≥21.7/22), which this environment's Node 20.11.1 does not have — attempting it fails at startup with a `SyntaxError`, not just an `engines` warning. Pinned `vitest@^3.2.7` instead (`vite` 5.x under the hood, Node `20.x` fully supported); `npm audit` reports 2 moderate `@vitest/mocker` advisories fixed only by `vitest@5` (a dev-only path-traversal/arbitrary-file-read risk in vitest's own local mock-transform server, not present in the built Next.js app or reachable in CI's non-interactive `vitest run`). Revisit the vitest version pin once this environment (or CI) runs Node ≥22. (An intermediate attempt at `vitest@5` briefly needed `npm install --legacy-peer-deps` to work around an unrelated npm 10.2.4 arborist bug — that workaround is gone now that the version is pinned to `^3.2.7`; a plain `npm install`/`npm ci` works, and both are verified in "Deployment prep" below.)
- Adopted ADR-0003 (static export deployment): set `output: "export"` in `next.config.ts` — every route was already static/SSG (Phase 1–5 never added an API route, server-only data fetch, or middleware), so no running Node server is actually needed in production. Changed `/lab` (`src/app/lab/page.tsx`) from a `next/navigation` `redirect()` to a real, thin landing page linking to `/lab/relayhub`, since a static export has no server to issue an HTTP redirect and `redirect()` would only work post-hydration (invisible to crawlers/no-JS).
- Deployment prep: added `Dockerfile` (multi-stage: `node:20-alpine` build → `nginx:1.27-alpine` runtime serving the static `out/` directory), `nginx.conf` (`try_files $uri $uri.html $uri/ =404;` — deliberately different from `cleanbrain-me-entrance`'s `try_files $uri $uri/ /index.html`, since `next export` emits `<route>.html` files and this site has no SPA fallback to hand unmatched paths to), `.dockerignore`, and `.github/workflows/deploy.yml` (test → build/push GHCR → SSH deploy gated by `ENABLE_PRODUCTION_DEPLOY`, matching `cleanbrain-me-entrance`'s CI/CD model exactly, with `namespace`/image names substituted).
- **Verified the deployment artifact for real, not just in theory**: ran `docker build` locally, started the resulting container, and confirmed via `curl` that every real route (including nested case-study/project detail pages) returns `200`, an unmatched path returns a real `404` with Next's own custom 404 page (via `error_page 404`), and `/_next/static/` assets carry long-cache headers. Then drove the RelayHub Live Lab in a real headless-Chromium session against that exact container (not `next dev`/`next start`) and generated an event successfully with no console errors — confirming the client-side adapter/state-machine logic survives being served as flat static files.
- Regenerated `package-lock.json` cleanly after the `vitest@5` experiment (which had left it inconsistent — `npm ci` failed with `EUSAGE`/a `picomatch` mismatch until this was fixed) and confirmed `npm ci` now succeeds, since that is what the CI workflow and `Dockerfile` both actually run.
- Updated `docs/architecture/overview.md`'s "Deployment target" (Node/container runtime → nginx static export), `docs/product/scope.md` (added the static-export decision to V1 in-scope), and `docs/infra-required-changes.md` with a materially more specific finding from actually reading `cleanbrain-me-infra`'s current README: the bare `developer.cleanbrain.me` hostname has **no DNS record and no Gateway listener at all** today (only its sibling `relayhub-java.developer.cleanbrain.me` exists), and a future `*.developer.cleanbrain.me` wildcard would **not** cover it (a DNS wildcard needs a label in the `*` position) — a dedicated A record is required regardless of what else gets added later. Removed the unused default `create-next-app` placeholder assets (`public/*.svg`) and the stale Vercel/Next.js boilerplate sections of `README.md`.
- Drafted and pushed the `cleanbrain-me-infra` manifests (`kubernetes/namespaces/cleanbrain-me-developer.yaml`, `kubernetes/apps/developer/{deployment,service,rbac,httproute}.yaml`), then added a `developer` service section to that repository's `README.md` mirroring `entrance`'s (Runtime, image, first-time deployment commands), plus updates to its Gateway listener list, DNS hostname list, resource budget table, and repository layout tree.
- The maintainer resolved DNS (`developer.cleanbrain.me` is a CNAME to `cleanbrain.me`) and, working from this session, added the `developer-https` Gateway listener via the documented `kubectl patch` mechanism — confirmed with `openssl s_client` (`CN = developer.cleanbrain.me`, real Let's Encrypt issuer, `Verify return code: 0`) before any application resources existed (cert-manager's Gateway Shim issues per-listener independently of whether the backend/HTTPRoute exists).
- **The maintainer then applied `namespace` → `rbac` → `deployment` → `service` → `httproute` directly on the server** (this session's harness blocked the equivalent `kubectl apply` as a "Production Deploy" action, so the maintainer ran it themselves and shared the terminal output). Verified from this session afterward: `pod/web` `Running 1/1`, `httproute/developer` `Accepted: True`/`ResolvedRefs: True`, `https://developer.cleanbrain.me/` (and `/lab/relayhub`, `/projects/relayhub`) return `200`, an unmatched path returns `404`, and a live headless-Chromium pass against the production URL generated a synthetic RelayHub event (Normal scenario, all six stages `success`) with no console errors. Updated `cleanbrain-me-infra`'s README to record all of this as confirmed, not pending.
- Wired up CI-driven deploys end to end: set `HETZNER_SSH_HOST`/`USER`/`PRIVATE_KEY`/`KNOWN_HOSTS` GitHub Actions secrets on this repo, reusing `english-core-speaking`'s existing CI SSH keypair (same server, same `deploy` Linux account) — the host key fingerprint was cross-checked directly on the server (`ssh-keygen -lf /etc/ssh/ssh_host_ed25519_key.pub`) before trusting `ssh-keyscan`'s network-captured output.
- Created the fourth-application CI ServiceAccount token (`ci-deployer-cleanbrain-me-developer-token`) and merged its context into `/home/deploy/.kube/config` alongside `english-core-speaking`, `cleanbrain-me-entrance`, and `relayhub-java`, without disturbing the existing `current-context`. Verified: `kubectl auth whoami` → `system:serviceaccount:cleanbrain-me-developer:ci-deployer`; `can-i patch deployment/web` → `yes`; `can-i get secrets` → `no`.
- Set `ENABLE_PRODUCTION_DEPLOY=true` and triggered a real workflow run (`34709418370`): `test` → `build-and-push` → `deploy` all succeeded. **Verified end to end**: the running Pod's image is the immutable commit-SHA tag CI just pushed (`ghcr.io/cleanbrain-developer/cleanbrain-me-developer:d24d822...`), not `:latest`, and `curl -I https://developer.cleanbrain.me/` still returns `200` afterward. The CI/CD pipeline works end to end, not just theoretically wired up. Updated `cleanbrain-me-infra`'s README ("developer" > "2."/"3.") to record this as confirmed.
- Replaced default favicon/icons with the maintainer's own pixel-art image (`src/app/{favicon.ico,icon.png,apple-icon.png}`, Next.js's file-based icon convention) — verified the generated `<link rel="icon"/"apple-touch-icon">` tags against the actual static export output and confirmed the new icon serves correctly in production.
- Per the maintainer's explicit request: `/` now redirects (client-side `next/navigation` `redirect()`, since static export has no server-side redirect — see ADR-0003) to `/lab/relayhub`, making the Live Lab the front door instead of the portfolio narrative. The former homepage content (Hero, Engineering Focus, Featured Projects, Live Lab Preview, Selected Case Studies, Architecture snapshot, Resume/Contact) was moved intact to a new `/profile` route rather than deleted, and the header logo now links there instead of `/` — nothing from Phase 2's content work was lost, it just isn't the default landing experience anymore. Verified in a real browser (via the built Docker image): loading `/` actually navigates to `/lab/relayhub`, and `/profile` renders the full former-homepage content correctly.
- Added ADR-0004 (live RelayHub observability) and `LiveObservabilityPanel` (`src/components/relayhub/live-observability/`, backed by `src/lib/relayhub/live-observability.ts`) on `/lab/relayhub`, under a "Beyond the simulation" heading: real, live data read directly from `relayhub-java`'s actual public endpoints (`/api/deliveries/summary`, `/api/targets`, `/api/metrics/query`) — genuinely real, not `MockRelayHubAdapter`, and deliberately not implemented as a `RelayHubAdapter` (that interface is for the interactive Lab's simulated event lifecycle; this is one-way, read-only telemetry with no equivalent mock). Labeled "Live · real data" with a visually distinct badge from the Mock Lab's "Synthetic demo data" badge, so the two can never be confused. Handles the loading/error/success states explicitly (design spec's "Error UX"), with a Retry/Refresh action.
- **This required a coordinated change to `relayhub-java` (a separate repository)**: that service had no CORS configuration at all, so a cross-origin browser fetch from `developer.cleanbrain.me` was blocked even though the underlying data was already public to any direct browser visit. Added a `CorsConfigurationSource` bean to `relayhub-java`'s `security/SecurityConfig.java`, scoped to exactly `https://developer.cleanbrain.me`, `GET` only, and only the four already-public path prefixes this panel needs — verified by reading that service's actual `SecurityConfig.java` and its `/api/metrics` proxy controller first (confirming the real PromQL metric names and JSON shapes used, e.g. `relayhub_ingress_events_total`, `{succeeded,pending,dead}`) rather than guessing. Could not compile-check locally (no JDK available in this environment) — relying on `relayhub-java`'s own CI `test` job (which gates `build-and-push`/`deploy`) to catch any real compile error before that change reaches production.
- Verified locally end to end via the actual Docker image (not just `next dev`): `/` redirects to `/lab/relayhub` in a real browser, `/profile` renders correctly, and `LiveObservabilityPanel` correctly shows its error/retry state (expected, since `relayhub-java`'s CORS change had not yet been deployed at verification time) rather than crashing the page.
- Pushed both repositories; verified on the real production site (not just locally): after `relayhub-java`'s CI (`test` → `build-and-push` → `deploy`) succeeded, `curl -H "Origin: https://developer.cleanbrain.me"` against `relayhub-java`'s real endpoints returned `Access-Control-Allow-Origin: https://developer.cleanbrain.me`, and a live headless-browser pass against `https://developer.cleanbrain.me/lab/relayhub` showed real numbers (e.g. `succeeded: 35,720`, not an error state).
- **Per the maintainer's explicit follow-up feedback** — confirmed unused, and the maintainer wanted the real data to be the eye-catching, attention-grabbing experience rather than plain numbers next to an unused simulator — **removed the entire Mock RelayHub Lab** (ADR-0005): deleted `RelayHubAdapter`/`MockRelayHubAdapter`/`HttpRelayHubAdapter` and all six Mock Lab component folders (`event-generator`, `pipeline`, `metrics`, `recent-events`, `event-detail`, `dlq`) along with their tests. Rebuilt `/lab/relayhub` around a new `LiveDashboard` (`src/components/relayhub/live-observability/live-dashboard.tsx`): large KPI tiles, two dependency-free inline-SVG `Sparkline` time-series charts (real Prometheus range queries for ingress rate and delivery-attempts-by-status, not accumulated client samples), a live delivery-targets list, a pulsing "live" indicator, and auto-refresh every 10 seconds with no loading-flash on refresh. Also removed the now-inconsistent hardcoded-fake-numbers `LiveLabPreview` component from `/profile` (replaced with a plain link to the real dashboard) — keeping fabricated stats next to a page that now shows real ones would have undercut the whole point.
- Extracted `parseInstantValue`/`parseRangeSeries` as pure, exported functions in `live-observability.ts` and added `live-observability.test.ts` (6 tests, fixtures drawn from real captured API responses) — replacing the unit-test coverage lost when the Mock Lab's own tests were deleted, so Phase 5's "first automated test suite" investment isn't simply gone.
- Replaced every remaining "Live Lab" / "generate a synthetic event" reference across the site (nav label, `/lab` landing page, `/architecture`'s adapter-boundary principle, `/projects/relayhub`'s content) with accurate "Live Monitoring" framing. Verified via `npm run lint`/`test`/`build` (all pass) and the actual Docker image + a real production headless-browser pass (screenshot confirms real KPI numbers, sparklines, and targets rendering correctly with no console errors).
- **Per further maintainer feedback** — "that's a dashboard, I wanted relayhub-java's actual Live screen; the UI should be visibly, dynamically moving the instant the site loads" — read `relayhub-java`'s real `frontend/src/pages/LivePage.tsx` and `styles.css` (that repo's admin-console Live page: an SSE-driven topology diagram with animated missile pulses, impact explosions, and node-hit shake effects) and ported it into this repository as `LiveTopology` (`src/lib/relayhub/live-topology.ts` + `src/components/relayhub/live-observability/live-topology.tsx`), rather than `<iframe>`-embedding the original (blocked by that page's own `X-Frame-Options: DENY`, confirmed directly). Opens a real cross-origin `EventSource` against `relayhub-java`'s `/api/live/stream` and renders live as ingress/delivery/DLQ events actually happen — genuinely animating on load, not a static snapshot. Ported CSS keyframes into `globals.css` (`topology-*`, `live-title-shimmer`), recolored to this site's own theme tokens rather than relayhub-java's. Narrower than the original by design: no login-gated demo-generator toggle (this site has no auth) and no click-through request/response detail on the activity feed.
- **Second, larger CORS expansion required on `relayhub-java`** (same day, same `CorsConfigurationSource` bean, still `GET`-only, still that one origin): added `/api/sources/**`, `/api/subscriptions/**`, `/api/dlq/**`, and `/api/live/**` (the SSE stream itself) — the topology diagram needs Sources/Targets/Subscriptions to lay out its nodes and the DLQ auto-replay countdown, on top of what the first CORS change already covered. Documented as an "Update" section on ADR-0004 rather than a new ADR, since it's the same decision (real, read-only, narrowly-scoped cross-origin access) extended, not a new one.
- `LiveObservabilityPanel`/its KPI-tiles-and-sparklines approach was **not deleted this time** — kept as `LiveDashboard`, now a secondary "Aggregate stats" section below `LiveTopology` on `/lab/relayhub`, for anyone who wants trend/summary numbers rather than watching individual events fly by. The topology animation is the page's primary, attention-grabbing element now; the dashboard is supplementary.
- Fixed two real lint findings while writing `LiveTopology` (not stylistic nitpicks): `react-hooks/purity` correctly flagged a `performance.now()` call inside a function reachable only from a `requestAnimationFrame` callback — the rule can't prove the call site, so it needed an explicit, justified `eslint-disable` comment rather than being silenced blindly; and three `delay > 0 ? window.setTimeout(spawn, delay) : spawn()` ternary-as-statement expressions (a pattern relayhub-java's own, differently-configured ESLint allows) were rewritten as explicit `if`/`else` to satisfy `@typescript-eslint/no-unused-expressions` here.
- Verified: `npm run lint`/`test`/`build` all pass. `relayhub-java`'s second CORS expansion was pushed and its CI (`test` → `build-and-push` → `deploy`, including the Testcontainers-backed integration suite) succeeded end to end; confirmed live with real `curl -H "Origin: https://developer.cleanbrain.me"` requests showing `Access-Control-Allow-Origin` on both a plain endpoint (`/api/sources`) and the SSE stream's own preflight (`/api/live/stream`).
- Added a mandatory Korean (`.ko.md`) companion for every `.md` document in this repository, per the maintainer's ADR-0004/ADR-0005 decision in `agent-dev-starter` (2026-09-17). English remains canonical for every pair.
- Copied `scripts/check-ko-companions.sh` from `agent-dev-starter` and added a step to `.github/workflows/deploy.yml`'s `test` job that runs it with `--missing-only` and fails the build on a missing `.ko.md` companion, per `agent-dev-starter`'s `ADR-0009` (2026-09-18).
- **Fixed a real bug the maintainer found in production**: DLQ items accumulated in `LiveTopology`'s counter but never resolved, even though `relayhub-java`'s own Live page correctly shows them clearing. Root cause: this repo's SSE handler only ever incremented `summary.dead` locally on a `dlq` stage event and never re-synced with the server, so it had no equivalent of `relayhub-java`'s own `scheduleDlqRefetch()` — a debounced re-fetch of `/api/deliveries/summary` and `/api/dlq/schedule`, triggered on a fresh `dlq` event *or* a `delivery` event with `replay: true` and `status: "success"` (a DLQ item that a `DlqAutoReplayScheduler` sweep just replayed successfully and removed from the queue). Re-read `relayhub-java`'s actual `frontend/src/pages/LivePage.tsx` and ported that exact mechanism: added `fetchDeliverySummary()` to `src/lib/relayhub/live-topology.ts`, and a debounced `scheduleSummaryRefetch()` in `live-topology.tsx` (wired to both the `dlq` branch and the replay-success case in the `delivery` branch), replacing the optimistic-only `dead: prev.dead + 1` increment that could only ever go up. Verified: `npm run lint`/`test`/`build` all pass.
- **Follow-up fix, same day**: the maintainer noticed replay missiles in `LiveTopology` still visibly launched from the Event node, not the DLQ node — `relayhub-java` had gained exactly this behavior the day before (`frontend/src/pages/LivePage.tsx`, "maintainer request 2026-09-18": a replay's pulse origin becomes `dlqPos` instead of `eventPos ?? hub`, since the item is actually leaving the DLQ, not arriving fresh) and this repo's earlier port predated that change. Ported the origin switch, plus the adjacent `bumpDeadCountAtImpact()` refinement from the same upstream commit — an impact-timed local `summary.dead` adjustment (+1 on a `dlq` event, -1 on a successful replay) that changes the visible count in step with the missile's actual landing, on top of (not instead of) the debounced `scheduleSummaryRefetch()` reconciliation added in the previous fix. Verified: `npm run lint`/`test`/`build` all pass.
- **Retrofitted the ADS V2 migration** (`agent-dev-starter` moved from its own V1 conventions to V2, built on open standards — the `AGENTS.md` standard, GitHub Spec Kit, and Agent Skills — in that repository's `ADR-0010` through `ADR-0013`, 2026-09-20). Mirrored the same migration into this repository:
  - Merged `.ai/constitution/agent-behavior.md`'s behavioral contract directly into `AGENTS.md` (now the sole, richer adapter) and deleted `agent-behavior.md`/`.ko.md`. `AGENTS.md` remains appended below Next.js's auto-managed `<!-- BEGIN/END:nextjs-agent-rules -->` block, undisturbed.
  - Merged `.ai/constitution/engineering-principles.md`'s content (including this repo's own demo-safety and RelayHub-integration principles) into a new `.specify/memory/constitution.md` — GitHub Spec Kit's own constitution role — and deleted `engineering-principles.md`/`.ko.md`.
  - Deleted `CLAUDE.md`/`CLAUDE.ko.md` entirely (`agent-dev-starter`'s `ADR-0011`): Claude Code has read `AGENTS.md` natively since v2.1.277 (2026-09-18), so a separate thin Claude adapter no longer earns its duplication cost. Checked `.github/workflows/deploy.yml` and `README.md` for load-bearing `CLAUDE.md` references — found only the `README.md` link, updated to point at `AGENTS.md` alone.
  - Updated `PROJECT.yaml`: `context.agent_entrypoints` (a `codex`/`claude_code` map) became the singular `context.agent_entrypoint: AGENTS.md`; added a `standards:` block (`agents_md`, `spec_kit.pinned_version: specify-cli==1.0.8`, `skills` paths) mirroring `agent-dev-starter`'s own; replaced `context.constitution` with `context.specify_constitution: .specify/memory/constitution.md` and `context.documentation_policy: .ai/constitution/documentation-policy.md`; formalized the already-pervasively-referenced `docs/infra-required-changes.md` as `context.infra_required_changes` (it was referenced from `AGENTS.md`, the architecture docs, and this file already, but never declared as a canonical `PROJECT.yaml` path). Replaced the `principles` list's `thin-agent-adapters` entry with `standards-over-reinvention` (now defined in `.specify/memory/constitution.md`), since a single-adapter model made the old principle's premise moot.
  - Updated `docs/architecture/repository-structure.md` and `docs/architecture/agent-context-model.md` to point at `AGENTS.md` as the sole adapter and `.specify/memory/constitution.md` as the constitution's new home; dropped the now-meaningless "differences between adapters" conflict-handling bullet. `docs/architecture/overview.md` needed no change — it had no ADS-convention references, only this project's own product/architecture facts.
  - Updated `docs/product/goals.md`'s "Success criteria" (bootstrap acceptance test) and `.ai/constitution/documentation-policy.md`'s "Single responsibility" list to the new file locations; updated `docs/infra-required-changes.md`'s citation of the old `engineering-principles.md` path.
  - Checked for a legacy `.ai/skills/` directory (`ADR-0003`'s original wording, corrected by `agent-dev-starter`'s `ADR-0012` to the real agent-discovered paths `.claude/skills/`/`.agents/skills/`) — none exists in this repository, so there was nothing to move.
  - Added `.ko.md` companions for every file touched in this migration (including the new `.specify/memory/constitution.md`) in the same change, per this repository's own mandatory bilingual policy; ran `bash scripts/check-ko-companions.sh --missing-only` and confirmed 0 missing before committing.
  - Left this repository's own `docs/decisions/ADR-0001` through `ADR-0005` untouched — they are this repository's own product/architecture decision log, unrelated to `agent-dev-starter`'s ADR numbering, and ADRs are not silently rewritten after acceptance. `ADR-0001`'s text still describes the old `AGENTS.md`+`CLAUDE.md` two-adapter model as of its 2026-09-12 decision date; that is now superseded in practice by this current-state entry, the same way `agent-dev-starter` records its own `ADR-0011` as a new decision rather than editing history.
  - **Deliberately did not install the real GitHub Spec Kit CLI.** This environment has no working Python/`uv`/`pip` (only a non-functional Windows Store stub `python.exe`), so `specify init` could not actually be run to render `.specify/`'s templates/scripts or real `.claude/skills/speckit-*/SKILL.md` / `.agents/skills/speckit-*/SKILL.md` files. Per `agent-dev-starter`'s `ADR-0010`, hand-writing those rendered files by guessing at Spec Kit's template source would risk shipping subtly broken skill files, so none were created. `.specify/memory/constitution.md` was hand-written instead — that is a durable-principles document, not CLI-rendered output, so it carries no such risk. See "Known constraints" and "Next" below for the follow-up this leaves open.
- **Closed the Spec Kit CLI installation gap the same day.** A real Python 3.12 + `uv` + `specify-cli==1.0.8` toolchain became available in this environment (2026-09-20), so the "Next" item below was run for real: `specify init --here --force --ignore-agent-tools --integration claude --script sh --non-interactive` followed by `specify integration install codex --force`. Verified `specify`'s own manifest tracking recognized the hand-written `.specify/memory/constitution.md` as already-customized and left it untouched — confirmed by an `md5sum` taken before and after the install (`733861adbfe2906f69fd388381093af1`, unchanged) and by re-reading the file's content (still this repository's real principles, not the generic `[PROJECT_NAME] Constitution` placeholder). Real `.specify/templates/`, `.specify/scripts/`, `.specify/workflows/`, `.specify/integrations/`, and real `.claude/skills/speckit-*/SKILL.md` / `.agents/skills/speckit-*/SKILL.md` files now exist, CLI-rendered rather than hand-written. Also copied the updated `scripts/check-ko-companions.sh` from `agent-dev-starter` (its new `ADR-0014`), which excludes these vendored Spec Kit paths and any `speckit-*`-prefixed skill from the `.ko.md` mandate — they are upstream content this repository does not author and that gets replaced wholesale on every `specify upgrade`, the same rationale already accepted for `node_modules`. `.specify/memory/constitution.md` and `.specify/memory/constitution.ko.md` remain mandatory and unaffected (the companion already existed from the initial V2 migration). Ran `bash scripts/check-ko-companions.sh --missing-only` and confirmed 0 missing after the change. See "Known constraints" below — the corresponding constraint entry is now resolved.

## In progress

- None currently. See Next.

## Next

1. Broaden the test suite: `live-observability.test.ts` covers the pure Prometheus-response parsing, but there is no route-level smoke test yet (`/`'s redirect, `/lab/relayhub` rendering the dashboard, `/profile` rendering the former homepage) — only manually verified via browser screenshots per change so far.
2. Decide whether to spend effort resolving the `vitest`/Node version mismatch (upgrade this environment's/CI's Node to ≥22 and move to `vitest@5`) versus staying on `vitest@3.2.7` — see "Known constraints". (Note: GitHub Actions' `actions/setup-node@v4` with `node-version: 20` in `.github/workflows/deploy.yml` gets a current Node 20.x patch release, not necessarily this local environment's 20.11.1 — worth double-checking whether CI's Node 20 also lacks `styleText`, or whether this is purely a local-environment constraint, before deciding.)
3. Consider whether the dashboard should surface more of `relayhub-java`'s available metrics over time — any addition needs its own deliberate CORS-allowlist entry on that service, per ADR-0004, not a blanket grant.
4. ~~Run a real `specify init --here --integration claude --integration codex`~~ **Done (2026-09-20, same day as the V2 migration this was deferred from)** — see "Completed" above. The CLI-generated install left `.specify/memory/constitution.md` untouched rather than overwriting it, so the reconciliation step anticipated here turned out not to be needed.
5. Consider whether `PROJECT.yaml`'s `delivery.cli_present: false` should flip to `true` now that `specify` is a real, working CLI in this environment — it currently only reflects whether this repository ships its own CLI product, not tooling installed in the dev environment, so double-check the field's intended meaning before changing it.

## Open decisions

See `docs/product/scope.md`, "Open decisions" (routing depth, whether to surface more `relayhub-java` metrics, and `cleanbrain-me-entrance`'s `planned`-status question — the `HttpRelayHubAdapter` timing question is moot now that ADR-0005 removed it entirely).

## Known constraints

- No backend, database, authentication, or CMS in V1 (see `docs/product/scope.md`).
- There is no mock/simulation UI on this site anymore (ADR-0005) — do not reintroduce a synthetic "generate an event" affordance without a new ADR explicitly reopening that question. The `relayhub-java` integration must stay `GET`-only and CORS-scoped to already-public endpoints; arbitrary URL, header, script, or credential input from a visitor must never be implemented (see `.ai/constitution/engineering-principles.md`).
- Target cluster is 2 vCPU / 4 GB RAM / 40 GB disk (per `cleanbrain-me-infra`) — the combined resource budget across all services already approaches this ceiling (see that repository's README and `cleanbrain-me-entrance`'s own `current-state.md`), so this app must stay lightweight.
- This repository owns application source, Dockerfile, and CI; `cleanbrain-me-infra` owns the production Kubernetes manifest — the two must not duplicate each other's content. Anything that looks like a required infra change is recorded in `docs/infra-required-changes.md`, never implemented here.
- No real chronological work-history timeline (company names, role titles, dates) exists anywhere on the site, by the maintainer's explicit choice — Experience/Resume are focus-area narratives only. Revisit only if the maintainer explicitly asks for a timeline later.
- Case studies are intentionally composite/abstracted with no real metrics — do not "fill in" numbers later without the maintainer explicitly providing real, disclosable ones.
- The application is a static export with no server runtime (ADR-0003) — any future feature needing genuine server-side logic (an API route, middleware, per-request server rendering) requires revisiting that ADR first, not just adding the feature.
- `LiveDashboard` creates a real runtime dependency on `relayhub-java`'s specific API/metric shape (field names in `/api/deliveries/summary` and `/api/targets`, the PromQL metric names `relayhub_ingress_events_total`/`relayhub_delivery_attempts_total`) — see ADR-0004. A breaking change to that API made without awareness of this dependency will silently break the dashboard (it degrades to an error/retry state, not a crash, but the coupling is real). It also polls every 10 seconds per open tab with no caching or rate limiting on this repository's side — fine at current traffic, worth remembering if that ever changes.
- `/` is a client-side-only redirect to `/lab/relayhub` (ADR-0003's static-export constraint) — a crawler or no-JS client sees `<head>` metadata for the site but not the interactive body. The full portfolio narrative previously at `/` now lives at `/profile`, linked from the header logo. There is no offline-safe fallback anymore if `relayhub-java` is down — a deliberate trade-off (see ADR-0005).
- `vitest` is pinned to `^3.2.7`, not latest, due to a real Node 20.11.1 incompatibility in `vitest@5`'s toolchain (see "Deployment prep" above) — do not bump it to `^5` without first confirming the runtime Node version supports it.
- ~~The GitHub Spec Kit CLI (`specify-cli==1.0.8`...) is not actually installed in this repository~~ **Resolved 2026-09-20**: a real Python 3.12 + `uv` + `specify-cli==1.0.8` toolchain is now installed, and `specify init`/`specify integration install codex` were actually run in this repository (see "Completed" above). `.specify/templates/`, `.specify/scripts/`, `.specify/workflows/`, `.specify/integrations/`, and real `.claude/skills/speckit-*/SKILL.md` / `.agents/skills/speckit-*/SKILL.md` files now exist, CLI-rendered rather than hand-written or absent. `.specify/memory/constitution.md` remains the same hand-written, project-authored file it was before (verified unchanged by `md5sum`) — Spec Kit's own manifest tracking correctly treated it as already-customized rather than overwriting it with the generic placeholder template.

## Exit criteria for this phase

- A fresh agent session, given only `AGENTS.md` or `CLAUDE.md`, correctly answers the five acceptance questions in `docs/product/goals.md`, each traceable to a repository path. (Met — 2026-09-12.)
- The foundation is committed as a reviewable baseline, with the maintainer's explicit confirmation for repository/remote creation and the first push. (Met.)
- Phase 1 (Foundation) application code exists and builds/lints cleanly, verified in a real browser at desktop and mobile widths. (Met.)
- Phase 2 (Portfolio Core) real content exists for every P0 route (Homepage, Experience, Projects × 2, Case Studies × 3, Architecture, Resume, Contact), builds/lints cleanly, and was spot-checked in a real browser. (Met.)
- Phase 3 (RelayHub Lab Mock) was functional end to end — all four scenarios and DLQ replay worked, backed by `MockRelayHubAdapter` behind the `RelayHubAdapter` interface, verified in a real browser including a mobile viewport. (Met at the time — **superseded 2026-09-13 by ADR-0005**, which removed this code entirely once confirmed unused. This line is kept as historical record, not current architecture.)
- Phase 4 (`HttpRelayHubAdapter` interface readiness) existed behind explicit, off-by-default configuration, with its contract documented in an ADR rather than assumed. (Met at the time — **superseded 2026-09-13 by ADR-0005**, same as Phase 3. `HttpRelayHubAdapter` no longer exists.)
- Phase 5 (Quality) has a first pass: per-page SEO metadata verified against real production output, and a first automated unit-test suite. (Met — the suite itself was rewritten 2026-09-13 when the Mock Lab's tests were deleted and replaced with `live-observability.test.ts`; the "broaden test coverage" item in Next remains open.)
- A real, deployable artifact exists and was verified locally end to end: `docker build` succeeds, the running container serves every route correctly (including a real 404 for unmatched paths), and the Live Monitoring dashboard is fully functional when served that way. (Met.)
- This site is actually live at `https://developer.cleanbrain.me`. (Met — 2026-09-13. Verified with real HTTP requests to multiple routes and a live headless-browser pass.)
- CI-driven deploys (`ENABLE_PRODUCTION_DEPLOY=true`) are verified working end to end, matching `cleanbrain-me-entrance`'s exit bar. (Met — 2026-09-13. A real triggered run succeeded, and the running Pod's image was confirmed to be the commit-SHA tag CI just deployed, not `:latest`.)
- `/lab/relayhub` shows real `relayhub-java` production data (not a mock, not an error state) on the live production site, confirmed via a real headless-browser pass after `relayhub-java`'s CORS change deployed. (Met — 2026-09-13.)
